# Customer Frontend (Vite)
# API base URL that the customer app will call
# In local dev, point to backend port 5001
VITE_API_URL=http://localhost:5001

# Cashfree Environment Configuration
# Use 'sandbox' for development, 'production' for live payments
VITE_CASHFREE_ENV=sandbox

# Production Example:
# VITE_API_URL=https://yourdomain.com
# VITE_CASHFREE_ENV=production
